import { formatBigIntToFixed } from '@kazama-defi/utils/formatBalance'
import { useCurrencyListData } from '../../api/CurrencyDataRetriever'

const CurrencyBar: React.FC = () => {
  const currencyData = useCurrencyListData()

  return null
}

export default CurrencyBar
